# News automation — πλάνο & roadmap

Το `news.json` γεμίζει αυτόματα από scheduled agent (Claude). Το frontend (`news.html` + `news.js`) απλώς το διαβάζει.

## Bylines / regions (σταθερά)
- **Στέφανος Ρήγας** → Ελλάδα (`region: "gr"`)
- **Νίκος Στοϊκος** → Εξωτερικό (`region: "intl"`)

## Τι τρέχει ΤΩΡΑ — Σαββατοκύριακα (scheduled task `flowless-weekend-news`, cron `0 9 * * 6,0`)
- **Σάββατο 09:00** → «Ανερχόμενοι της βδομάδας» (1 ΕΛ + 1 ΕΞΩ). Αν δεν βρει πραγματικό όνομα με ουσία → skip, κανένα post.
- **Κυριακή 09:00** → «Σαν σήμερα» (μουσική επέτειος) + roundup «τι κυκλοφόρησε το ΣΚ» (ΕΛ + ΕΞΩ).
- Κανόνας: καμία εφεύρεση, μόνο επιβεβαιωμένα από πηγές· αλλιώς κανένα post. Auto commit + push στο `main`.

## TODO — να προστεθεί (Δευτέρα + Παρασκευή, ΟΧΙ καθημερινά)
Το «καθημερινά» κρίθηκε υπερβολή (τα charts κουνιούνται λίγο μέρα-με-μέρα). Αντ' αυτού, hard news 2 φορές τη βδομάδα:
- **Δευτέρα 09:00** → «Η βδομάδα ξεκινά»: charts update (kworb GR daily/weekly, Billboard), τι έγινε/κορυφές. ΕΛ + ΕΞΩ.
- **Παρασκευή 09:00** → «New Music Friday»: τα σημερινά releases (release day), ΕΛ + ΕΞΩ. Πηγές: rapnroll, Billboard, Spotify New Music.

Έτσι η εβδομάδα καλύπτεται: **Δευ** (charts) · **Παρ** (releases) · **Σάβ** (ανερχόμενοι) · **Κυρ** (σαν σήμερα + recap). 4 μέρες, όχι 7.

### Πώς να στηθεί
Νέο scheduled task (ή επέκταση) με cron `0 9 * * 1,5` (Δευ+Παρ), ίδιο schema/κανόνες με το ΣΚ. Οι κανόνες (schema, «καμία εφεύρεση», commit μόνο αν υπάρχει ≥1 post) παραμένουν ίδιοι.
